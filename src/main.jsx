import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './hooks/products/index'
import 'antd/dist/reset.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  // <React.StrictMode>
    <App />
  // </React.StrictMode>,
)
